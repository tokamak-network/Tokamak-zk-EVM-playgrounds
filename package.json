{
  "name": "playground-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "lerna run dev",
    "build": "lerna run build",
    "test": "lerna run test",
    "lint": "lerna run lint"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "lerna": "^8.0.0",
    "prettier": "^3.0.0"
  }
}
